# Repository collaboration rules

- Only the human repository owner may create commits or push to GitHub.
- The coding agent may edit files and run tests, but must not run `git commit`,
  `git amend`, `git merge`, or `git push`.
- Do not add coding-agent identity as an author or contributor in project files.
- Do not rewrite or force-push history automatically.
